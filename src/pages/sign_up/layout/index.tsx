export const SignUpPageLayout = ({children}) => {

    return (
        <div className={'flex items-center justify-center h-[100vh]'}>
            <div>
                {children}
            </div>
        </div>
    )
}