import React, { useEffect } from 'react';

const AuthCallback = () => {
    useEffect(() => {
        // Handle user login success here
    }, []);

    return (
        <div>Logging in...</div>
    );
};

export default AuthCallback;
